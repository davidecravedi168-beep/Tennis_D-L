# Tennis Edge Pro — iOS

This directory contains the native iOS shell used for TestFlight/App Store readiness.

Release gates:
- XcodeGen project generation must succeed.
- iOS Simulator compilation must succeed.
- Production web/data endpoints must fail closed when stale or unreachable.
- Commercial data rights, billing, account/auth, notifications and App Store metadata remain separate pre-release gates.
