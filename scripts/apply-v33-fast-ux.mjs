import fs from 'node:fs';

const file='premium-shell-v1.js';
let s=fs.readFileSync(file,'utf8');
const original=s;

const replaceOnce=(oldText,newText,label)=>{
  if(s.includes(newText)) return;
  if(!s.includes(oldText)) throw new Error(`V33 patch contract changed: ${label}`);
  s=s.replace(oldText,newText);
};

replaceOnce("const VERSION='TEP_FULL_PREMIUM_UI_V3_1';","const VERSION='TEP_FULL_PREMIUM_UI_V3_3';",'version');
replaceOnce('if(window.__TEP_FULL_PREMIUM_UI_V31)return;','if(window.__TEP_FULL_PREMIUM_UI_V33)return;','guard-read');
replaceOnce('window.__TEP_FULL_PREMIUM_UI_V31=true;','window.__TEP_FULL_PREMIUM_UI_V33=true;','guard-write');

const stateMarker="const savePhotoCache=()=>{try{localStorage.setItem(photoKey,JSON.stringify(photoCache))}catch{}};";
const stateEnhanced=stateMarker+"\nconst photoInflight=new Map(),photoMisses=new Set();let photoObserver=null;";
replaceOnce(stateMarker,stateEnhanced,'photo-state');

const oldCandidates=`async function wikiCandidates(raw){\n  const name=pretty(raw),key=name.toLowerCase();\n  if(!name||/^(wsf|r16p|qf|sf)\\d+/i.test(name))return[];\n  if(photoCache[key])return[photoCache[key]];\n  const out=[];\n  try{const title=encodeURIComponent(name.replace(/\\s+/g,'_')),r=await fetch(\`https://en.wikipedia.org/api/rest_v1/page/summary/\${title}\`,{cache:'force-cache',headers:{Accept:'application/json'}});if(r.ok){const j=await r.json();const src=j?.thumbnail?.source||j?.originalimage?.source;if(src)out.push(src)}}catch{}\n  try{const q=encodeURIComponent(\`\${name} tennis\`),u=\`https://en.wikipedia.org/w/api.php?action=query&generator=search&gsrsearch=\${q}&gsrlimit=4&prop=pageimages&piprop=thumbnail&pithumbsize=800&format=json&origin=*\`,r=await fetch(u,{cache:'force-cache'});if(r.ok){const j=await r.json();for(const p of Object.values(j?.query?.pages||{})){const src=p?.thumbnail?.source;if(src&&!out.includes(src))out.push(src)}}}catch{}\n  return out;\n}`;
const newCandidates=`async function wikiCandidates(raw){\n  const name=pretty(raw),key=name.toLowerCase();\n  if(!name||/^(wsf|r16p|qf|sf)\\d+/i.test(name))return[];\n  if(photoCache[key])return[photoCache[key]];\n  if(photoMisses.has(key))return[];\n  if(photoInflight.has(key))return photoInflight.get(key);\n  const job=(async()=>{\n    const out=[];\n    try{const title=encodeURIComponent(name.replace(/\\s+/g,'_')),r=await fetch(\`https://en.wikipedia.org/api/rest_v1/page/summary/\${title}\`,{cache:'force-cache',headers:{Accept:'application/json'}});if(r.ok){const j=await r.json();const src=j?.thumbnail?.source||j?.originalimage?.source;if(src)out.push(src)}}catch{}\n    if(!out.length){\n      try{const q=encodeURIComponent(name),u=\`https://www.wikidata.org/w/api.php?action=wbsearchentities&search=\${q}&language=en&uselang=en&limit=6&format=json&origin=*\`,r=await fetch(u,{cache:'force-cache'});if(r.ok){const j=await r.json(),hit=(j?.search||[]).find(x=>/tennis/i.test(String(x?.description||'')));if(hit?.id){const e=await fetch(\`https://www.wikidata.org/w/api.php?action=wbgetentities&ids=\${encodeURIComponent(hit.id)}&props=claims&format=json&origin=*\`,{cache:'force-cache'});if(e.ok){const ej=await e.json(),file=ej?.entities?.[hit.id]?.claims?.P18?.[0]?.mainsnak?.datavalue?.value;if(file){const src=\`https://commons.wikimedia.org/wiki/Special:FilePath/\${encodeURIComponent(String(file).replace(/ /g,'_'))}?width=900\`;out.push(src)}}}}}catch{}\n    }\n    try{const q=encodeURIComponent(\`\${name} tennis\`),u=\`https://en.wikipedia.org/w/api.php?action=query&generator=search&gsrsearch=\${q}&gsrlimit=4&prop=pageimages&piprop=thumbnail&pithumbsize=800&format=json&origin=*\`,r=await fetch(u,{cache:'force-cache'});if(r.ok){const j=await r.json();for(const p of Object.values(j?.query?.pages||{})){const src=p?.thumbnail?.source;if(src&&!out.includes(src))out.push(src)}}}catch{}\n    if(!out.length)photoMisses.add(key);\n    return out;\n  })();\n  photoInflight.set(key,job);\n  try{return await job}finally{photoInflight.delete(key)}\n}`;
replaceOnce(oldCandidates,newCandidates,'photo-candidates');

const oldHydrate=`function avatar(name,cls=''){const eager=String(cls).includes('hero')?'eager':'lazy';return\`<span class=\"tp-avatar \${cls}\" data-photo=\"\${esc(name)}\"><span>\${esc(initials(name))}</span><img alt=\"\${esc(pretty(name))}\" loading=\"\${eager}\" decoding=\"async\"></span>\`}\nasync function tryImage(el,img,src){return new Promise(resolve=>{let done=false;const finish=ok=>{if(done)return;done=true;img.onload=null;img.onerror=null;if(ok)el.classList.add('has-photo');else el.classList.remove('has-photo');resolve(ok)};img.onload=()=>finish(img.naturalWidth>80&&img.naturalHeight>80);img.onerror=()=>finish(false);img.src=src;if(img.complete&&img.naturalWidth>80&&img.naturalHeight>80)finish(true);setTimeout(()=>finish(img.complete&&img.naturalWidth>80&&img.naturalHeight>80),5000)})}\nasync function hydrate(root=document){\n  const nodes=$$('[data-photo]',root).filter(x=>x.dataset.photoState!=='loading'&&x.dataset.photoState!=='done');\n  await Promise.all(nodes.map(async el=>{\n    el.dataset.photoState='loading';const img=$('img',el);if(!img){el.dataset.photoState='done';return}\n    const name=el.dataset.photo,key=pretty(name).toLowerCase(),candidates=await wikiCandidates(name);let ok=false;\n    for(const src of candidates){if(await tryImage(el,img,src)){photoCache[key]=src;savePhotoCache();ok=true;break}}\n    if(!ok){el.classList.remove('has-photo');img.removeAttribute('src');delete photoCache[key];savePhotoCache()}\n    el.dataset.photoState='done';\n  }));\n}`;
const newHydrate=`function avatar(name,cls=''){const eager=String(cls).includes('hero')?'eager':'lazy';return\`<span class=\"tp-avatar \${cls}\" data-photo=\"\${esc(name)}\"><span>\${esc(initials(name))}</span><img alt=\"\${esc(pretty(name))}\" loading=\"\${eager}\" decoding=\"async\" fetchpriority=\"\${eager==='eager'?'high':'low'}\"></span>\`}\nasync function tryImage(el,img,src){return new Promise(resolve=>{let done=false;const finish=ok=>{if(done)return;done=true;img.onload=null;img.onerror=null;if(ok)el.classList.add('has-photo');else el.classList.remove('has-photo');resolve(ok)};img.onload=()=>finish(img.naturalWidth>80&&img.naturalHeight>80);img.onerror=()=>finish(false);img.src=src;if(img.complete&&img.naturalWidth>80&&img.naturalHeight>80)finish(true);setTimeout(()=>finish(img.complete&&img.naturalWidth>80&&img.naturalHeight>80),4500)})}\nasync function loadAvatar(el){\n  if(!el||el.dataset.photoState==='loading'||el.dataset.photoState==='done')return;\n  el.dataset.photoState='loading';const img=$('img',el);if(!img){el.dataset.photoState='done';return}\n  const name=el.dataset.photo,key=pretty(name).toLowerCase(),hadCached=!!photoCache[key];let candidates=await wikiCandidates(name),ok=false;\n  for(const src of candidates){if(await tryImage(el,img,src)){photoCache[key]=src;savePhotoCache();ok=true;break}}\n  if(!ok&&hadCached){delete photoCache[key];savePhotoCache();candidates=await wikiCandidates(name);for(const src of candidates){if(await tryImage(el,img,src)){photoCache[key]=src;savePhotoCache();ok=true;break}}}\n  if(!ok){el.classList.remove('has-photo');img.removeAttribute('src');delete photoCache[key];photoMisses.add(key);savePhotoCache()}\n  el.dataset.photoState='done';\n}\nfunction hydrate(root=document){\n  photoObserver?.disconnect();photoObserver=null;\n  const nodes=$$('[data-photo]',root).filter(x=>x.dataset.photoState!=='loading'&&x.dataset.photoState!=='done'),immediate=[],deferred=[];\n  for(const el of nodes){const key=pretty(el.dataset.photo).toLowerCase(),r=el.getBoundingClientRect();(photoCache[key]||el.classList.contains('hero')||r.top<window.innerHeight+260?immediate:deferred).push(el)}\n  void Promise.allSettled(immediate.map(loadAvatar));\n  if(!deferred.length)return;\n  if('IntersectionObserver'in window){photoObserver=new IntersectionObserver(entries=>{for(const entry of entries){if(entry.isIntersecting){photoObserver?.unobserve(entry.target);void loadAvatar(entry.target)}}},{rootMargin:'360px 0px'});deferred.forEach(el=>photoObserver.observe(el))}\n  else setTimeout(()=>deferred.forEach(el=>void loadAvatar(el)),0);\n}`;
replaceOnce(oldHydrate,newHydrate,'photo-hydration');

replaceOnce('<strong>Top Match</strong><span>Probabilità e contesto.</span>','<strong>Tutte le partite</strong><span>Cerca, filtra e confronta.</span>','home-quick-copy');
replaceOnce('<div class="tp-page-title"><h1>Esplora</h1><p>Cerca e confronta i prossimi match.</p></div>','<div class="tp-page-title"><h1>Partite</h1><p>Tutti i prossimi match, con filtri chiari.</p></div>','explore-title');
replaceOnce('<div class="tp-page-title"><h1>Le Mie Analisi</h1><p>Preferiti, giocatori seguiti e performance.</p></div>','<div class="tp-page-title"><h1>Salvati e seguiti</h1><p>I tuoi match preferiti, giocatori seguiti e risultati.</p></div>','mine-title');
replaceOnce('<button data-route="explore"><i>⌕</i><span>Esplora</span></button>','<button data-route="explore"><i>🎾</i><span>Partite</span></button>','nav-explore');
replaceOnce('<button data-route="analysis"><i>☆</i><span>Analisi</span></button>','<button data-route="analysis"><i>◎</i><span>Analisi</span></button>','nav-analysis');
replaceOnce('<button data-route="mine"><i>▥</i><span>Le Mie Analisi</span></button>','<button data-route="mine"><i>★</i><span>Salvati</span></button>','nav-mine');

const oldRender=`function applyView(){const view=$('#tpView');if(!view)return;view.innerHTML=screenHtml();$$('.tp-nav button').forEach(b=>b.classList.toggle('on',b.dataset.route===state.screen));const fresh=$('.tp-fresh');if(fresh)fresh.textContent=freshness();hydrate(view)}\nfunction render(transition=true){const view=$('#tpView');if(!view)return;if(transition&&document.startViewTransition){document.startViewTransition(()=>applyView())}else{applyView();if(transition){view.classList.remove('tp-enter');void view.offsetWidth;view.classList.add('tp-enter')}}}\nfunction route(name){const next=name||'home';state.scroll[state.screen]=window.scrollY;state.screen=next;closeMenu();render(true);requestAnimationFrame(()=>window.scrollTo({top:state.scroll[next]||0,left:0,behavior:'auto'}))}`;
const newRender=`function applyView(){const view=$('#tpView');if(!view)return;view.innerHTML=screenHtml();$$('.tp-nav button').forEach(b=>b.classList.toggle('on',b.dataset.route===state.screen));const fresh=$('.tp-fresh');if(fresh)fresh.textContent=freshness();queueMicrotask(()=>hydrate(view))}\nfunction render(transition=true){const view=$('#tpView');if(!view)return;applyView();if(transition){view.classList.remove('tp-enter');void view.offsetWidth;view.classList.add('tp-enter')}}\nfunction route(name){const next=name||'home';if(next===state.screen){requestAnimationFrame(()=>window.scrollTo({top:0,left:0,behavior:'auto'}));return}state.scroll[state.screen]=window.scrollY;state.screen=next;closeMenu();$$('.tp-nav button').forEach(b=>b.classList.toggle('on',b.dataset.route===next));render(false);requestAnimationFrame(()=>window.scrollTo({top:state.scroll[next]||0,left:0,behavior:'auto'}))}`;
replaceOnce(oldRender,newRender,'instant-navigation');

replaceOnce('let searchTimer=null;','let searchTimer=null,navPointerAt=-1e9;','nav-pointer-state');
replaceOnce("root.addEventListener('click',e=>{const r=e.target.closest('[data-route]');if(r){route(r.dataset.route);return}","root.addEventListener('pointerdown',e=>{const r=e.target.closest('.tp-nav [data-route]');if(!r)return;navPointerAt=performance.now();e.preventDefault();route(r.dataset.route)});\n  root.addEventListener('click',e=>{const r=e.target.closest('[data-route]');if(r){if(r.closest('.tp-nav')&&performance.now()-navPointerAt<700)return;route(r.dataset.route);return}",'nav-pointer-binding');

replaceOnce("#tpView.tp-enter{animation:tpEnter .14s ease-out}@keyframes tpEnter{from{opacity:.72;transform:translateY(5px)}to{opacity:1;transform:none}}","#tpView.tp-enter{animation:tpEnter .08s ease-out}@keyframes tpEnter{from{opacity:.86;transform:translateY(2px)}to{opacity:1;transform:none}}",'fast-enter-css');
replaceOnce(".tp-nav button{border:0;background:none;color:#9caec2;display:grid;place-items:center;gap:2px;min-height:54px;transition:color .12s ease,transform .12s ease}.tp-nav i{font-size:22px;font-style:normal}.tp-nav span{font-size:8px}.tp-nav button.on{color:var(--tp-lime)}",".tp-nav button{border:0;background:transparent;color:#9caec2;display:grid;place-items:center;gap:2px;min-height:54px;border-radius:14px;transition:color .08s ease,transform .08s ease,background .08s ease}.tp-nav i{font-size:22px;font-style:normal}.tp-nav span{font-size:9px;font-weight:800;letter-spacing:.01em}.tp-nav button.on{color:var(--tp-lime);background:rgba(202,255,61,.09)}",'clear-nav-css');
const mediaMarker='@media(min-width:720px){';
const visibilityRule='@supports (content-visibility:auto){.tp-match-card,.tp-live-card,.tp-record-list>div{content-visibility:auto;contain-intrinsic-size:92px}}\n';
if(!s.includes(visibilityRule.trim())){
  if(!s.includes(mediaMarker))throw new Error('V33 patch contract changed: media-marker');
  s=s.replace(mediaMarker,visibilityRule+mediaMarker);
}

for(const required of [
  "TEP_FULL_PREMIUM_UI_V3_3",
  'www.wikidata.org/w/api.php?action=wbsearchentities',
  "rootMargin:'360px 0px'",
  "queueMicrotask(()=>hydrate(view))",
  "root.addEventListener('pointerdown'",
  '<span>Partite</span>',
  '<span>Salvati</span>',
  '<h1>Partite</h1>',
  '<h1>Salvati e seguiti</h1>'
]) if(!s.includes(required))throw new Error('V33 required marker missing: '+required);
if(s.includes('document.startViewTransition'))throw new Error('V33 slow navigation path still present');

if(s!==original){fs.writeFileSync(file,s);console.log('V33_FAST_UX_PATCH=APPLIED')}else console.log('V33_FAST_UX_PATCH=ALREADY_APPLIED');
