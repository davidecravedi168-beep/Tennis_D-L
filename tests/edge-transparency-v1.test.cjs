const test=require('node:test'),assert=require('node:assert/strict'),cp=require('node:child_process'),fs=require('node:fs');
test('edge transparency is bounded, explainable and non-authorizing',()=>{cp.execFileSync(process.execPath,['scripts/edge-transparency-v1.mjs']);const x=JSON.parse(fs.readFileSync('data/edge-transparency-v1.json'));assert.ok(x.edge_score.value===null||(x.edge_score.value>=0&&x.edge_score.value<=100));assert.match(x.edge_score.note,/does not authorize/i);assert.ok(Object.keys(x.edge_score.components).length>=3);for(const r of x.why_not)assert.ok(r.reason&&Number.isFinite(r.count));assert.ok(x.evidence_sha256?.length===64)});
