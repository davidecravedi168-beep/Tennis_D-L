const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs');const s=fs.readFileSync('premium-shell-v1.js','utf8');test('Free Pro preview cannot charge or alter model',()=>{for(const x of ['SIMULAZIONE','Nessun pagamento attivo','Nessun paywall reale','Free / Pro Preview'])assert.ok(s.includes(x),x);for(const bad of ['stripe','checkout.session','payment_intent','paypal'])assert.equal(s.toLowerCase().includes(bad),false,bad);const nav=(s.match(/<nav class="tp-nav"[\s\S]*?<\/nav>/)||[''])[0];assert.equal((nav.match(/data-route=/g)||[]).length,4)});