const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs');const s=fs.readFileSync('premium-shell-v1.js','utf8');test('product readiness stays minimal and evidence-first',()=>{for(const x of ['30 SECONDI','Probabilità','Edge','Verifica','VERIFIED PROOF','Prematch lock','Auditabile'])assert.ok(s.includes(x),x);assert.ok(s.includes("localStorage.setItem(onboardKey,'1')"));assert.equal((s.match(/<nav class="tp-nav"[\s\S]*?<\/nav>/)||[''])[0].match(/data-route=/g).length,4)});