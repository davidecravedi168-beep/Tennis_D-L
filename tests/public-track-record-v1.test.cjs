const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),cp=require('node:child_process'),crypto=require('node:crypto');
test('public track record is generated from evidence and self-verifies',()=>{cp.execFileSync(process.execPath,['scripts/public-track-record-v1.mjs']);const x=JSON.parse(fs.readFileSync('data/public-track-record-v1.json'));const hash=x.evidence_sha256;delete x.evidence_sha256;assert.equal(hash,crypto.createHash('sha256').update(JSON.stringify(x)).digest('hex'));assert.equal(x.methodology.paper_only,true);assert.equal(x.methodology.strict_nulls,true);assert.ok(x.performance.closed>=0);assert.ok(x.clv.coverage>=0&&x.clv.coverage<=1);assert.equal(x.clv.sample,x.clv.provider_sample+x.clv.proxy_sample);assert.ok(x.probability_quality.brier===null||(x.probability_quality.brier>=0&&x.probability_quality.brier<=1))});
