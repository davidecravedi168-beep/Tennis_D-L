const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs');const s=fs.readFileSync('scripts/clv-tracker-v3.mjs','utf8');test('CLV capture stays genuine and near-start',()=>{assert.match(s,/CLOSING_WINDOW_MIN=180/);assert.match(s,/sideOdds\(p,side\)/);assert.match(s,/age>CLOSING_WINDOW_MIN/);assert.match(s,/matches_with_observation_last_60m/);assert.match(s,/No synthetic or retroactive closing prices/);assert.doesNotMatch(s,/source:\s*['\"](?:SYNTHETIC|INTERPOLATED)['\"]/i)});