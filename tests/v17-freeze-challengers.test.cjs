const test=require('node:test'),assert=require('node:assert/strict'),cp=require('node:child_process'),fs=require('node:fs');test('challenger hypotheses are frozen prospective and cannot auto promote',()=>{cp.execFileSync(process.execPath,['scripts/v17-freeze-challengers.mjs']);const x=JSON.parse(fs.readFileSync('data/v17-challenger-hypotheses.json'));assert.equal(x.champion_unchanged,true);assert.match(x.status,/FROZEN/);assert.equal(x.hypotheses.length,2);for(const h of x.hypotheses){assert.equal(h.prospective_only_after,x.frozen_at);assert.equal(h.promotion,'HUMAN_REVIEW_ONLY');assert.ok(h.min_settled>=50);assert.ok(h.primary_metrics.includes('CLV'));assert.ok(h.guardrails.includes('Brier'))}});