const test=require('node:test'),assert=require('node:assert/strict'),cp=require('node:child_process'),fs=require('node:fs');test('V17 segmentation remains diagnostic only',()=>{cp.execFileSync(process.execPath,['scripts/v17-loss-segmentation.mjs']);const x=JSON.parse(fs.readFileSync('data/v17-loss-segmentation.json'));assert.equal(x.policy.diagnostic_only,true);assert.equal(x.policy.no_threshold_changes,true);assert.equal(x.policy.no_champion_changes,true);for(const k of ['surface','odds','confidence','data_quality','verdict','tournament'])assert.ok(Array.isArray(x.segments[k]));});